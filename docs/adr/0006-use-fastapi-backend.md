# Use FastAPI for the Local Backend

The local backend is built with Python FastAPI and managed with uv. FastAPI fits the local API shape for image uploads, Ollama proxying, structured response validation, and cancellation support, and Python leaves room for future image-processing modules without changing the application boundary.
