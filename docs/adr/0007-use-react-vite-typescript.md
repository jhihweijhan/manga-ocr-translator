# Use React, Vite, and TypeScript for the WebUI

The WebUI is built with React, Vite, and TypeScript. The interface needs local application state for uploads, model selection, processing cancellation, editable OCR text, and translation comparison, while Vite keeps the first local version lightweight compared with a full application framework.
